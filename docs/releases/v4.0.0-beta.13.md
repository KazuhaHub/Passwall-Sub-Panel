# PSP v4.0.0-beta.13

## 绿联 NAS Docker 安装兼容性

- Docker 安装下载的凭据文件改名为 `node-credential.txt`，避免部分 NAS 文件管理器将无扩展名路径误识别或创建为文件夹。
- Compose 的凭据挂载路径与下载文件名保持一致。
- 更新中英文安装向导，明确 Docker 项目目录中应放置 `compose.yaml` 和 `node-credential.txt`。
- 手动安装仍使用原有的 `node-credential` 文件名，不影响现有手动部署流程。
